# Maintainer Mode plugin bundle

This directory is the installable Codex plugin referenced by the repository marketplace. For installation, usage, architecture, and security boundaries, see the [repository README](../../README.md).

The plugin contains four skills and a zero-dependency Python runner. It makes no network request unless the user explicitly invokes the read-only GitHub snapshot command.
